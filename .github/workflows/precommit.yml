name: Check Title

on:
  pull_request:
    branches:
      - main

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run check_title action
        uses: ./.github/actions/check_title
